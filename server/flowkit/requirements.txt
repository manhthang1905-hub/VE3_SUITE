# FlowKit Server - FULL dependencies
# (gateway + agent + login Chrome + doc account/sheet)
# ============================================================

# --- Gateway / Agent (uvicorn gateway:app, agent/main.py) ---
fastapi>=0.104.0
uvicorn>=0.24.0
websockets>=12.0
httpx>=0.25.0
pyyaml>=6.0
requests>=2.28.0

# --- Login Chrome + 2FA + clipboard ---
DrissionPage>=4.0.0
pyotp>=2.9.0
pyperclip>=1.8.2

# --- Doc danh sach account / Google Sheet ---
openpyxl>=3.1.0
gspread>=6.1.2
google-auth>=2.35.0
google-auth-oauthlib>=1.2.0
google-api-python-client>=2.140.0
