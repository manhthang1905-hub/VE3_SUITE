# VE3 Studio - FULL runtime dependencies
# (worker tao anh/video + GUI + agent/gateway + auth extension + NGUON sheet)
# ============================================================================

# --- Core: Excel, config, HTTP, GUI ---
openpyxl>=3.1.0
PyYAML>=6.0
requests>=2.28.0
Pillow>=9.0.0
customtkinter>=5.2.0
psutil>=5.9.0

# --- Agent / Gateway (port 8100/5100) ---
fastapi>=0.104.0
uvicorn>=0.24.0
websockets>=12.0
httpx>=0.25.0
flask>=3.0.0

# --- NGUON (Google Sheet) — thieu se loi: No module named google.oauth2 ---
gspread>=6.1.2
google-auth>=2.35.0
google-auth-oauthlib>=1.2.0
google-api-python-client>=2.140.0

# --- Browser auth / Flow extension (login, TOTP, Chrome automation) ---
selenium>=4.15.0
undetected-chromedriver>=3.5.0
webdriver-manager>=4.0.0
DrissionPage>=4.0.0
pyautogui>=0.9.54
pyperclip>=1.8.2
pyotp>=2.9.0

# --- Windows clipboard / COM (win32clipboard, win32com, win32con) ---
pywin32>=306
